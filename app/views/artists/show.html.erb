<h1><%= @artist.name %></h1>

<% if !@songs.empty? %>
    <ul>
        <% @songs.each do |song| %>
            <li>
                <%= link_to song.title, song_path(song) %>
            </li>
        <% end %>
    </ul>
<% end %>